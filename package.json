{
  "name": "lia-gcode-preview",
  "version": "1.0.0",
  "scripts": {
    "build": "npx parcel build src/index.ts",
    "watch": "npx parcel serve src/index.html --open",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "gcode-preview": "^2.17.0"
  },
  "devDependencies": {
    "parcel": "^2.12.0"
  }
}
